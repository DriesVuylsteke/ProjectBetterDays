using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// Creates a gui to represent the job priorities. This means the order of the children of buttonsParent should be organised according to the priorites retrieved from the jobQueue
/// </summary>

public class CreateWorkPriorityGui : MonoBehaviour
{
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    

    // Update is called once per frame
    void Update()
    {
        
    }
}
